from setuptools import setup, find_packages

setup(
    name='rl',
    packages = ['rl'],
    version='0.0.1',
    url='https://github.com/5121eun/rl.git',
    install_requires=[
        'torch'
    ]
)